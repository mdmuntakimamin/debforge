# Plugins System

This directory provides an extensible plugin system.
Plugins can hook into build and packaging stages without
modifying core logic.

Plugins are disabled by default.
Enable them by linking into the `enabled/` directory.
